import "package:flutter/material.dart";

class AppMeasurement {
  final k_Padding_top = const Padding(padding: EdgeInsets.only(top: 10));
  final k_padding_left = const Padding(padding: EdgeInsets.only(left: 10));

  const AppMeasurement();
}
